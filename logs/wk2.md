Things I don't understand:

- What does `<!DOCTYPE html>` mean?
- What does `<meta charset="UTF-8">` do?
- What does `<meta name="viewport" content="width=device-width, initial-scale=1.0">` do?
- What is the difference between `<head>` and `<body>`?
- What does the `href` attribute in an `<a>` tag do?

What I worked on:
This week, I learned the basics of HTML and CSS and practiced creating and styling a webpage.
I also practiced reading AI-generated code and understanding what different parts of the code do.

What gave me trouble?
I had some trouble remembering HTML syntax and understanding how HTML and CSS work together.
I am still getting used to finding where errors are in my code.

Something new I picked up
I learned that HTML controls the structure of a webpage, while CSS controls how it looks.

Working with AI
I noticed that AI is more helpful when I ask it to explain the code instead of just asking it to give me the answer.

